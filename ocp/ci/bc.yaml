kind: "BuildConfig"
apiVersion: "v1"
metadata:
  name: "poc1-producer"
spec:
  source:
    git:
      uri: "https://github.com/Dimss/poc1.git"
  strategy:
    jenkinsPipelineStrategy:
      jenkinsfile:
    type: JenkinsPipeline